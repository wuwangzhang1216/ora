## Build / packaging outputs — never committed
.xcode-build/
.app/
.dist/
.build/
.swiftpm/
# Package.resolved IS committed: the manifest tracks moving branches
# (mlx-swift-lm main), so without the lockfile every fresh checkout
# resolves different upstream code and builds are not reproducible.

## Local-only docs (personal secrets, credential flow)
PACKAGING.md

## macOS junk
.DS_Store
