.venv/
.mls/
__pycache__/
*.pyc
.DS_Store

# macOS Swift app — source is closed; only the notarized Ora.dmg ships in releases
macos/

# iOS app — source is also closed; ships via TestFlight / App Store
ios/
